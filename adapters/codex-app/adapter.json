{
  "host": "codex-app",
  "displayName": "Codex App",
  "integrationPriority": "plugin-first",
  "plugin": {
    "mode": "plugin",
    "skillsPath": "skills",
    "manifest": ".codex-plugin/plugin.json"
  },
  "skills": {
    "mode": "plugin",
    "path": "skills"
  },
  "instructions": {
    "plugin": ".codex-plugin",
    "requiresBackup": true
  },
  "superpowers": {
    "required": true,
    "install": "Install Superpowers from the Codex plugin marketplace."
  },
  "checks": [
    "superpowers",
    "skills-catalog",
    "plugin-install",
    "codex-plugin-manifest",
    "compatibility-bridge-optional"
  ],
  "compatibilityBridge": {
    "optional": true,
    "protocol": "mcp",
    "transport": "stdio",
    "command": "dyslexai",
    "args": [
      "mcp"
    ],
    "note": "Use only for hosts or workflows that cannot consume the skills plugin directly."
  }
}
